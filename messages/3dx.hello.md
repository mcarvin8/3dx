# summary

Print a greeting. Reference command demonstrating this template's conventions: oclif command class, `@salesforce/core` Messages for i18n, flags, and a typed JSON result.

# description

Print a greeting. This is the one example command shipped with the 3dx template — replace it with your plugin's first real command, or use it as a reference for the file layout (command class, message file, unit test, NUT) when adding new ones.

# examples

- `sf 3dx hello`
- `sf 3dx hello --name "World"`

# flags.name.summary

Name to greet. Defaults to "World".
